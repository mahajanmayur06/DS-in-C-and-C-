#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<int> mergerKSortedArrays (vector<vector<int>>& arr) {
    int n = arr.size();
    vector<int> ans;
    priority_queue<int, vector<int>, greater<int>> pq;
}

int main()
{
    
    return 0;
}